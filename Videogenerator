{previewData && (
  <div style={{
    width: '360px',
    height: '640px',
    position: 'relative',
    overflow: 'hidden',
    borderRadius: '12px',
    backgroundImage: `url(${previewData.bgImageUrl})`,
    backgroundSize: 'cover',
    backgroundPosition: 'center',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    padding: '20px',
    textAlign: 'center'
  }}>
    <audio src={previewData.audioUrl} autoPlay loop controls style={{ position: 'absolute', bottom: '20px', width: '80%' }} />
    <div style={{
      backgroundColor: 'rgba(0,0,0,0.6)',
      color: 'white',
      padding: '15px',
      borderRadius: '8px',
      fontSize: '18px',
      fontWeight: 'bold'
    }}>
      <p style={{ margin: '0 0 10px 0', color: '#ffd700' }}>{previewData.hook}</p>
      <p style={{ margin: 0 }}>{previewData.body}</p>
    </div>
  </div>
)}
